APP_ENV=local
APP_DEBUG=0

# Generate a random value of at least 32 characters for every environment.
# Never commit the real value.
BUBBLE_ROYALE_SESSION_SECRET=replace-with-a-random-32-character-minimum-secret
